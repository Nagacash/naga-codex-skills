# Sub-agents

Use sub-agents only when they improve the task: repository exploration, independent
research, test planning, security review, documentation review, parallel investigations.

Each sub-agent must have a narrow purpose, defined output, limited scope, no unnecessary
secret access, no authority to deploy production, change permissions, merge, or push
without approval.

The main agent must summarize and verify sub-agent results rather than blindly accepting them.
